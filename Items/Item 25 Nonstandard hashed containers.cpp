/*
 * Hashed containers (unordered containers)
 * 
 * Unordered containers:
 *  - unordered_map
 *  - unordered_set
 *  - unordered_multimap
 *  - unordered_multiset
 *  
 * - They are still associative containers, just unordered:
 *  - Therefore -> Comparison function for objects is required, but this is based on EQUALITY, NOT equivalence, so its default equal_to
 *
 *
 * Summary:
 *  -
 */